{
  "name": "huasMD",
  "version": "0.1.6",
  "description": "Agent Native Markdown 编辑器 — Markdown as Database，支持中英文切换、多主题、目录导航",
  "main": "dist/main/index.js",
  "author": {
    "name": "ikevss",
    "email": "hello@huasmd.dev"
  },
  "license": "MIT",
  "homepage": "https://github.com/ikevss/huasMD",
  "repository": {
    "type": "git",
    "url": "https://github.com/ikevss/huasMD.git"
  },
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "preview": "electron-vite preview",
    "dist": "electron-vite build && electron-builder",
    "dist:mac": "electron-vite build && electron-builder --mac",
    "dist:win": "electron-vite build && electron-builder --win",
    "dist:linux": "electron-vite build && electron-builder --linux"
  },
  "dependencies": {
    "@milkdown/kit": "^7.19.2",
    "electron-updater": "^6.8.9",
    "pptxgenjs": "^4.0.1",
    "remark-breaks": "^4.0.0"
  },
  "devDependencies": {
    "electron": "^34.0.0",
    "electron-builder": "^26.0.0",
    "electron-vite": "^3.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
